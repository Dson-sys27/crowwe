-->Crowwe game on construction:

Current advancements:
+ Created world map rendered form a fil and printed from WordGraphicsDrawer
+ Added a PlayerClaracter which is controlled with WASD keys around the map
+ Camera that follows the PlayerCharacter
+ Prop as streetlight added
+ Colllision system added (needs revision)

On building:
+ Builders to create entities. These read a JSON file where metadata is stored
+ Logging and a regulated exceptions control
+ Robusted way to generate/destroy entities
+ Map collisions
+ Animated sprites
+ 